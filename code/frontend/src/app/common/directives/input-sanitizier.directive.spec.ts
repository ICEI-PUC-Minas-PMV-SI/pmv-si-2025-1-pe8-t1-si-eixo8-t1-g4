import { InputSanitizierDirective } from './input-sanitizier.directive';

describe('InputSanitizierDirective', () => {
  it('should create an instance', () => {
    const directive = new InputSanitizierDirective();
    expect(directive).toBeTruthy();
  });
});
